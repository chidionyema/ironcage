[package]
name = "ironcage-inference"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

# llama.cpp bindings: using llama-crab or direct C bindings
# Placeholder for now; swap with actual llama-cpp-rs when available
